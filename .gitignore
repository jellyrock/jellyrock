.env
*.log
# BrightScript 
dist/apps
out/
build/
# Device-run ledger: accumulates across runs, so it must NOT live under out/
# (every `npm run build*` starts with `rimraf out/`). See scripts/run-record.js.
.device-runs/
build-analysis/
# roku-deploy drops a 10-byte case-sensitivity probe in the repo ROOT (random hex
# suffix) and does not always clean it up — every sideload can leave one, so it
# shows up as untracked and blocks any clean-tree gate (`/pr`'s pre-flight).
ROKU_DEPLOY_CASE_CHECK_*.TXT
roku_modules
# Rooibos
bsconfig-tdd.json
# NPM 
node_modules/
# Eclipse
.buildpath
.project
.settings

.claudesync
/tasks/
.claude/logs/
.claude/handoffs/
.claude/plans/
# server-upgrade-automation: raw spec + transient diff/findings caches.
# Committed fingerprints (docs/architecture/spec-fingerprints/) and the
# committed suppression config (.api-watch/suppressions.yml) are NOT ignored.
.api-watch/cache/
# Project PLANs are local agent-continuity (like .claude/handoffs, .claude/plans);
# public repo keeps them out of git. Lifecycle skills still run locally.
docs/projects/
