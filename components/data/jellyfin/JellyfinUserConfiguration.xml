<?xml version="1.0" encoding="utf-8"?>
<!--
  Jellyfin User Configuration

  This content node represents the Configuration object from the Jellyfin server API.
  Configuration data is SERVER-AUTHORITATIVE and session-specific - it should NEVER
  be loaded from or synced to the registry.

  These settings are controlled by the Jellyfin server and may change between sessions.
  The server is the single source of truth for this data.

  LOADING STATE PATTERN: SET ONCE AT LOGIN
  - Populated once during user.Login() from server API response
  - Never modified after initial load
  - Never synced to registry
-->
<component name="JellyfinUserConfiguration" extends="ContentNode">
  <interface>
    <!-- Audio Settings -->
    <field id="audioLanguagePreference" type="string" />
    <field id="playDefaultAudioTrack" type="boolean" />
    <field id="rememberAudioSelections" type="boolean" />

    <!-- Subtitle Settings -->
    <field id="subtitleMode" type="string" />
    <field id="subtitleLanguagePreference" type="string" />
    <field id="rememberSubtitleSelections" type="boolean" />

    <!-- Display Settings -->
    <field id="displayMissingEpisodes" type="boolean" />
    <field id="displayCollectionsView" type="boolean" />
    <field id="hidePlayedInLatest" type="boolean" />
    <field id="useEpisodeImagesInNextUpAndResume" type="boolean" />
    <field id="showBackdrop" type="boolean" />

    <!-- Playback Settings -->
    <field id="enableNextEpisodeAutoPlay" type="boolean" />

    <!-- Media Segment Action Preferences (from web client DisplayPreferences CustomPrefs) -->
    <field id="segmentActionCommercial" type="string" />
    <field id="segmentActionIntro" type="string" />
    <field id="segmentActionOutro" type="string" />
    <field id="segmentActionPreview" type="string" />
    <field id="segmentActionRecap" type="string" />
    <field id="segmentActionUnknown" type="string" />

    <!-- View Organization -->
    <field id="orderedViews" type="array" />
    <field id="latestItemsExcludes" type="array" />
    <field id="myMediaExcludes" type="array" />
    <field id="groupedFolders" type="array" />

    <!-- Other Settings -->
    <field id="castReceiverId" type="string" />
    <field id="enableLocalPassword" type="boolean" />

    <!-- DEBUG ONLY: Raw configuration data for debugging - only populated when bs_const=debug=true -->
    <field id="rawConfigData" type="assocarray" />
  </interface>
</component>