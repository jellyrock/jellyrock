# Build output / vendor / generated.
node_modules/
build/
build-analysis/
out/
**/roku_modules/

# npm-owned (npm install rewrites these; let it).
package-lock.json

# Translation tooling owns these (scripts/lint/update-translations.cjs).
locale/

# Vendor JSON snapshots — generated upstream, mirrored here.
tasks/jellyfin-server-openapi/
scripts/lint/issue-forms.schema.json

# Generated by scripts/generate/api-usage-manifest.js (drift-checked by
# docs:api-manifest:check; JSON.stringify output, not prettier's array wrapping).
docs/architecture/api-usage-manifest.json

# Generated by scripts/generate/spec-fingerprint.js (drift-checked by
# docs:spec-fingerprints:check; reduced upstream spec surface, JSON.stringify
# output — large, deterministic, not prettier's to reflow).
docs/architecture/spec-fingerprints/

# Gitignored / per-machine.
.claude/settings.local.json

# CHANGELOG is CI-controlled (see CLAUDE.md "cannot modify").
CHANGELOG.md

# BrighterScript / BrightScript / XML — bsfmt and the XML formatter own these.
*.bs
*.brs
*.xml
manifest

# Markdown — markdownlint owns these.
*.md

# YAML — out of scope for this initiative; let GitHub Actions / yamllint own
# any future YAML formatting. Re-evaluating to fold YAML into Prettier scope
# would be its own follow-up because of the cross-cutting workflow churn.
*.yml
*.yaml
